// Display a popup with the message "Hello World"
alert("Hello World");

// Output a message to the console
console.log("i am urooj");

// Prompt the user to enter a number and store the input in variable 'a'
var a = prompt("Enter your number");
// Log the value entered by the user to the console
console.log(a);

// Ask the user if they want to close the app and store their response in 'isTrue'
var isTrue = confirm("DO you want to close the App");

// Check if the user confirmed (clicked OK) to close the app
if (isTrue) {
    // Output a message indicating that the app is closing
    console.log("App is closed");
} else {
    // Output a message indicating that the user should continue using the app
    console.log("Continue using the app");
}
